{
  "name": "monorepo-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": ["my-shared", "tasks/*"],
  "scripts": {
    "build": "tsc --build ./tsconfig.json"
  }
}
